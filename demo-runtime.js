(()=>{const root=document.querySelector('.demo-workbench');if(!root)return;const buttons=[...root.querySelectorAll('.demo-choice button')],summary=document.getElementById('demo-summary'),done=document.getElementById('demo-complete'),result=document.getElementById('demo-result');const sync=()=>{const picked=[...root.querySelectorAll('.demo-choice .is-selected')].map(x=>x.textContent.trim());summary.textContent=picked.join(' → ');if(result)result.hidden=true;};buttons.forEach(btn=>btn.addEventListener('click',()=>{const group=btn.closest('.demo-choice');group.querySelectorAll('button').forEach(x=>x.classList.remove('is-selected'));btn.classList.add('is-selected');sync();}));done?.addEventListener('click',()=>{sync();if(result){result.hidden=false;result.scrollIntoView({behavior:'smooth',block:'nearest'});}});sync();})();