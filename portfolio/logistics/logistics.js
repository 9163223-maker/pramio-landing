const form=document.querySelector('#trackForm');const input=document.querySelector('#trackInput');const result=document.querySelector('#trackResult');form.addEventListener('submit',e=>{e.preventDefault();const value=input.value.trim().toUpperCase();if(value==='NL-2408'){result.textContent='Рейс найден · в пути · прибытие сегодня в 18:40';input.removeAttribute('aria-invalid')}else{result.textContent='Демо-режим: попробуйте номер NL-2408';input.setAttribute('aria-invalid','true')}});input.addEventListener('input',()=>input.removeAttribute('aria-invalid'));