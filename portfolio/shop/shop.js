const cart=[];const count=document.querySelector('#count'),drawer=document.querySelector('#drawer'),items=document.querySelector('#items');document.querySelectorAll('.buy').forEach(b=>b.onclick=()=>{cart.push(b.dataset.name);count.textContent=cart.length;render();drawer.hidden=false});document.querySelector('.cart').onclick=()=>{drawer.hidden=!drawer.hidden};document.querySelector('#close').onclick=()=>drawer.hidden=true;function render(){items.innerHTML=cart.length?cart.map((x,i)=>'<p>'+x+' <button data-i="'+i+'">×</button></p>').join(''):'<p>Корзина пуста</p>';items.querySelectorAll('button').forEach(b=>b.onclick=()=>{cart.splice(+b.dataset.i,1);count.textContent=cart.length;render()})}document.querySelectorAll('.filters button').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filters button').forEach(x=>x.classList.remove('on'));b.classList.add('on');const f=b.dataset.filter;document.querySelectorAll('.product').forEach(p=>p.hidden=f!=='all'&&p.dataset.cat!==f)});