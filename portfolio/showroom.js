document.querySelectorAll('.showroom-card').forEach(card=>{const link=card.querySelector('.proof-link');if(!link)return;card.addEventListener('click',event=>{if(event.target.closest('a,button,input,select,textarea'))return;link.click()});const title=card.querySelector('h2,h3')?.textContent||'демо';link.setAttribute('aria-label','Открыть проект: '+title)});if(location.hash){const el=document.querySelector(location.hash);if(el)setTimeout(()=>el.scrollIntoView({block:'center'}),80)}