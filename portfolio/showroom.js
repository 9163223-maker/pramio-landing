const hydrateCard=card=>{card.querySelectorAll('iframe[data-src]').forEach(frame=>{frame.src=frame.dataset.src;frame.removeAttribute('data-src')})};const previewObserver='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;hydrateCard(entry.target);previewObserver.unobserve(entry.target)}),{rootMargin:'240px 0px'}):null;document.querySelectorAll('.showroom-card').forEach(card=>{if(previewObserver)previewObserver.observe(card);else hydrateCard(card);const link=card.querySelector('.proof-link');if(!link)return;const title=card.querySelector('h2,h3')?.textContent||'демо';link.setAttribute('aria-label','Открыть проект: '+title);const remember=()=>{try{sessionStorage.setItem('pramio_showroom_return',card.id||'catalog')}catch{}};const go=()=>{remember();location.href=link.href};card.addEventListener('click',e=>{if(e.defaultPrevented||e.button>0||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;if(e.target.closest('button,input,select,textarea,label,a'))return;go()});card.addEventListener('keydown',e=>{if(e.target!==card||(e.key!=='Enter'&&e.key!==' '))return;e.preventDefault();go()});link.addEventListener('click',()=>remember())});if(location.hash){const el=document.querySelector(location.hash);if(el)setTimeout(()=>el.scrollIntoView({block:'center'}),80)}