document.querySelectorAll('.showroom-card').forEach(card=>{const link=card.querySelector('.proof-link');if(!link)return;card.setAttribute('role','link');card.setAttribute('aria-label','Открыть: '+(card.querySelector('h2,h3')?.textContent||'демонстрационный проект'));const go=()=>{location.href=link.href};card.addEventListener('click',e=>{if(e.target.closest('a,button,input,select,textarea'))return;go()});card.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();go()}});link.addEventListener('click',()=>{const id=link.getAttribute('href').split('/').filter(Boolean).pop();sessionStorage.setItem('pramio_showroom_return',id||'catalog')})});if(location.hash){const el=document.querySelector(location.hash);if(el)setTimeout(()=>el.scrollIntoView({block:'center'}),80)}