const hydrateCard=card=>{card.querySelectorAll('iframe[data-src]').forEach(frame=>{frame.src=frame.dataset.src;frame.removeAttribute('data-src')})};const previewObserver='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;hydrateCard(entry.target);previewObserver.unobserve(entry.target)}),{rootMargin:'240px 0px'}):null;document.querySelectorAll('.showroom-card').forEach(card=>{if(previewObserver)previewObserver.observe(card);else hydrateCard(card);const link=card.querySelector('.proof-link');if(!link)return;card.addEventListener('click',event=>{if(event.target.closest('a,button,input,select,textarea,iframe'))return;link.click()});const title=card.querySelector('h2,h3')?.textContent||'демо';link.setAttribute('aria-label','Открыть проект: '+title)});if(location.hash){const el=document.querySelector(location.hash);if(el)setTimeout(()=>el.scrollIntoView({block:'center'}),80)}