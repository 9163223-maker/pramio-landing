document.querySelectorAll('.showroom-card').forEach(card=>{const link=card.querySelector('.proof-link');if(!link)return;card.setAttribute('role','link');card.setAttribute('aria-label','Открыть проект: '+(card.querySelector('h2,h3')?.textContent||'демо'));const remember=()=>sessionStorage.setItem('pramio_showroom_return',card.id||'catalog');const go=()=>{remember();location.href=link.href};card.addEventListener('click',e=>{if(e.defaultPrevented||e.button>0||e.metaKey||e.ctrlKey||e.shiftKey||e.altKey)return;if(e.target.closest('button,input,select,textarea,label,a'))return;go()});card.addEventListener('keydown',e=>{if(e.target!==card||(e.key!=='Enter'&&e.key!==' '))return;e.preventDefault();go()});link.addEventListener('click',()=>remember())});if(location.hash){const el=document.querySelector(location.hash);if(el)setTimeout(()=>el.scrollIntoView({block:'center'}),80)}